<Page
    x:Class="SimulationPlatform.Pages.TrackPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimulationPlatform.Pages">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="20,0,0,0">
            <TextBlock Text="Track Page"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   FontSize="32"/>
            <TextBlock Text="Control a vehicle on a virtual track."
					   FontSize="14"
					   Foreground="DarkGray"/>
			<TextBlock
				Text="UNDER CONSTRUCTION"
				FontSize="18"
				Foreground="IndianRed"/>
        </StackPanel>
		
		<!-- Temporary inputs to test and demo the track mechanics, replace with driving images-->
		<StackPanel Grid.Row="1" Margin="20,10,0,0">
			<Button
				Content="Accelerate"
				Click="AccelerateButton_Click"/>
		</StackPanel>
		<!-- Dashboard stack panel, show some of the data in a nice "dashboard" format-->
		<StackPanel>
			<!-- TODO -->
		</StackPanel>
	</Grid>
</Page>
